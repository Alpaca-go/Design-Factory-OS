# 2026-07-20

## Masterpiece OS — 执行向视觉方向 v2 实验分支开发

按《Masterpiece OS Execution-oriented Visual Direction v2 实验分支开发文档》完成 v2 实验实现。

- 分支：`experiment/execution-oriented-directions-v2`（基线 tag `sprint1-mvp-v1.3.3`，基于 `feature/visual-translation-v1` 提交 b404c76/1.3.3）。
- 新增 `src/v5/visual-translation/v2/`（config / schemas / runtime / prompts / report / index），**未修改任何 v1 代码**。
- 核心契约：`direction-contract-v2.js`（Direction Contract v2，含 industry_recognition_layer、core_reusable_assets、composition_templates、anti_concept_art_constraints）、`anchor-contract-v2.js`。
- 核心逻辑：`execution-readiness-evaluator.js`（9 项 1–5 指标 + 通过标准 + rewrite_required）、`anti-concept-art-constraints.js`（9 条防概念稿检测 + 地产/抽象漂移）、`regression-guards.js`（资产权限/证据保护/受众边界）、`ab-runner.js`（conceptual_v1 vs execution_oriented_v2）。
- 测试：`tests/v5/visual-translation-v2.test.js`（14 用例）；fixtures 在 `tests/fixtures/visual-direction-v2/{jiuzhou-meixue,mingjitang,vanke-suwan}/`；snapshots 在 `tests/snapshots/visual-direction-v2/`。
- 结果：`node --test tests/v5/*.test.js` → 117 passed / 0 failed（含既有的 103 个 v1 用例，无回归）。A/B 三项目均 pass，`runABRunner` 建议 `candidate_for_merge`。
- 交付文档：`docs/visual-direction-v2-execution-delivery.md`（对应开发文档第十三节 16 项交付）。

关键约束（勿违反）：v2 只重构视觉转译末端；禁止修改 Document Extraction / Brand Fact / Evidence Index / Audience Boundary / Asset Authorization / Direction Score v1 / Difference Matrix v1 / Report Compiler v1.3.3 / Checkpoint v1 / Sprint 2 Runtime v1 / Provider Adapter / Desktop / Phase 4B；不直连图片生成。

- 提交：已 commit 至实验分支 `experiment/execution-oriented-directions-v2`，commit `d426dda`（39 files, +4675/-81），工作区干净。未 push、未合并。

## v2 接入桌面客户端并重新打包 portable

用户要求"把 V2 接入客户端，然后重新打包一个客户端"。

- 新增 `src/v5/visual-translation/v2/runtime/run-visual-translation-v2.js`：复用 v1 上游 00/01/02（prepareDocumentSet、buildVisualEvidencePrompt、buildVisualSignalOpportunityPrompt 及各自 validator），仅替换 04（buildExecutionDirectionV2Prompt + validateExecutionDirectionV2 → compileExecutionDirectionV2）与 10（compileExecutionDirectionsReportV2）。返回结构与 v1 对齐：`{reportMarkdown, metrics, composition, modelCallCount, status, protocolVersion:'visual-translation-v2-execution', reportBasename:'visual-directions-report-v2-experimental.md'}`。v1 零改动。
- 桌面端 `apps/desktop/src/main/visual-translation-service.ts`：新增 `v2Runner` 参数（默认 runVisualTranslationV2）；`execute()` 读取 `settings.directionGenerationMode`（默认 conceptual_v1，归一化后选 runner），按 `execution.execution.reportBasename`/`protocolVersion` 动态生成报告文件名与 run-report 的 protocolVersion。STAGE_MESSAGES 增加 `04b-compile-execution-directions`。
- 设置层：`shared/types.ts` 新增 `DirectionGenerationMode` 类型，把 `directionGenerationMode` 加进 `PublicSettings`(可选，兼容测试) 与 `SaveSettingsInput`(可选)，并把 `04b-compile-execution-directions` 加入 `VisualTranslationStage` 联合类型；`settings-store.ts` 的 `StoredSettings` 持久化该字段（默认 conceptual_v1），`saveSettings` 仅在传入时更新。
- UI：`VisualTranslationWorkspace.tsx` 增加"方向生成模式"开关（概念方向 V1 生产 / 执行向 V2 实验），切换时落盘 `settings.save({...directionGenerationMode})`；开始按钮与标题随模式变化；报告页 eyebrow 按 `reportFilename` 含 `v2-experimental` 显示 V2。`styles.css` 增加 `.mode-toggle` / `.mode-hint` 样式。
- 校验：`node --test tests/v5/visual-translation-v2.test.js` → 14/14 通过；`npm run verify:document-flows`（离线闸门）→ 3/3 通过且 Desktop TypeScript contracts PASS；`apps/desktop` 全量 `tsc --noEmit` 通过。
- 打包：用 `env -u CODEBUDDY_SAFE_DELETE_BULK_STATE_DIR -u CODEBUDDY_TOOL_CALL_ID npm run desktop:package`（规避 electron-builder 清空 locales 触发的 WorkBuddy 批量删除确认拦截）。产物预期 `apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`。

注意：v1 生产管线字节级未变，无需真实 provider 端到端冒烟（AGENTS.md 闸门仅在生产分析 prompt/provider/schema/retry/checkpoint/report 改动时触发；本次仅新增隔离的 v2 路径并切换选择逻辑）。

## v2 Stage 04 输出截断修复（P0+P1）

用户用 V2 分析时客户端报 `模型输出达到长度上限，结构化 JSON 被截断`（qwen3.6-plus 在 stage 04 返回被截断的 JSON）。按《Masterpiece OS v2 Stage 04 输出截断修复开发文档》修复：

- 根因：v2 大型 schema（约 v1 的 2–3 倍）错误复用了 v1 的 `04-three-creative-directions` Stage Profile（maxOutputTokens 8000）。
- P0：`src/v5/visual-translation/v1/protocol/stage-registry.js` 新增独立 profile `04-execution-oriented-directions-v2`（thinking:true, thinkingBudget:2500, maxOutputTokens:20000, requestTimeoutMs:420000, truncationRetry:{enabled,maxAttempts:1,multiplier:1.5}），**v1 的 8000/5000/300000 原样保留**；新增 `getStageProfile(id)`。
- v2 Runner：`run-visual-translation-v2.js` 的 `model()` 在 stage 04 显式选 v2 profile + `getModelCapabilities(input.provider, input.modelId)`，重写后支持：①请求前 `resolveMaxOutputTokens` 上限校验（未知模型跳过）；②每次生成记录 token 诊断（promptTokens/completionTokens/finishReason/requestedMaxOutputTokens/providerMaxOutputTokens/outputUtilization，利用率≥85% 写 `OUTPUT_TOKEN_BUDGET_NEAR_LIMIT`）；③一次升级预算重试（截断时 maxOutputTokens×1.5，封顶 Provider 上限；仍截断抛 `OUTPUT_TRUNCATED_AFTER_RETRY`）。
- 新增模块：`src/v5/adapters/model-capabilities.js`（MODEL_CAPABILITIES 注册表【qwen3.6-plus 假设 32768，文档要求以真实 Provider 为准】、getModelCapabilities、resolveMaxOutputTokens、planTruncationRetry、ModelCapabilityError）；`src/v5/visual-translation/v2/runtime/output-budget.js`（estimateStage04V2OutputBudget、outputUtilization）。
- P1 Schema 压缩：`direction-contract-v2.js` 收紧长度上限（visual_description 400→240、execution_role 300→160、business_evidence 400→240、prohibited_use 300→200；其余 400 描述字段统一→300）。
- P1 Desktop 错误映射：`visual-translation-service.ts` 新增 `mapVisualTranslationUserError`（OUTPUT_TRUNCATED / OUTPUT_TRUNCATED_AFTER_RETRY / MODEL_OUTPUT_LIMIT_EXCEEDED → 含失败阶段/模型/请求预算/Provider 上限/是否重试/建议），`execute()` 的 catch 落盘 `userError` 并挂到抛出错误上；`shared/types.ts` 加 `VisualTranslationUserError`；`VisualTranslationWorkspace.tsx` 新增 `TruncationErrorNotice` 渲染结构化错误（不再只显示红框）；`styles.css` 加 `.truncation-error` 样式。
- 测试：新增 `tests/v5/visual-translation-v2-stage04.test.js`（8 用例，含通过预置 01/02 checkpoint 隔离测试 stage 04 截断恢复/二次截断/Profile 选择/上限校验/预算预估）。`node --test` v2 全量 22/22 通过；`verify:document-flows` PASS；desktop typecheck 通过。
- 再次打包（同 safe-delete workaround）产物 `apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`。
- 待办/已知限制：qwen3.6-plus 真实 maxOutputTokens 为假设值，真实运行后需按 OUTPUT_TOKEN_BUDGET_NEAR_LIMIT 诊断回填 MODEL_CAPABILITIES；若 20k 仍不稳，按文档 P2 拆 5 次调用。
- 打包完成确认（22:33，BUILD_EXIT=0）：`apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`，111,953,987 字节（≈112MB）。已验证修复代码打进 bundle：main `out/main/index.js` 命中 `04-execution-oriented-directions-v2`×5、`resolveMaxOutputTokens/planTruncationRetry/MODEL_OUTPUT_LIMIT_EXCEEDED/OUTPUT_TRUNCATED_AFTER_RETRY`×11；renderer bundle 命中 `requestedMaxOutputTokens/providerMaxOutputTokens`×4（结构化错误 UI）。

## v2 执行向视觉方向专项修复（按《Masterpiece OS v2 执行向视觉方向专项修复开发文档》）

用户要求按文档修复：过度合规信息化（E01/E02/E03 同族）、品牌身份污染（「安迹」）、方向同质化、合规权重过高、B2B2C 覆盖不足、评分逻辑 bug `100/100 + rewrite_required`。

新增文件（`src/v5/visual-translation/v2/runtime/`）：
- `evaluator-keywords.js`：六道 Gate 共用词表（BUSINESS_MODEL_DIMENSIONS / COMPLIANCE_WEIGHT_KEYWORDS / INDUSTRY_RECOGNITION_CATEGORIES / BRAND_NAME_SUFFIX【仅强品牌后缀，避免「医美/美学」误报】/ FORGERY_PATTERNS / BRAND_ROLE_KEYWORDS / STRATEGIC_THESIS_KEYWORDS / countKeywordHits）。
- `direction-text-util.js`：collectDirectionText 收集方向内全部用户可见字符串。
- `brand-identity-preservation-evaluator.js`：P0 `detectUnexpectedBrandNames`（denylist + 强后缀扫描）+ Gate（brand_name/role/strategic_thesis/industry_identity preserved，contamination_detected → UNEXPECTED_BRAND_IDENTITY）。
- `business-model-coverage-evaluator.js`：每方向覆盖 upstream/platform/institution/consumer 四类中 ≥3、整体须覆盖全部 4 类。
- `direction-family-difference-evaluator.js`：Jaccard 仅对**具体行业词汇**（industry_recognition_layer 5 类 + asset_name + photo real_industry_objects，排除 composition/example 的 cosmetic subject），阈值 0.72；声明 family 须 distinct。
- `compliance-weight-controller.js`：deriveComplianceWeights（关键词密度归一化）；evaluateComplianceWeight（最多 1 primary ≥0.5、3 secondary；全 ≥0.5 → overweight；新增 `compliance_supplychain_dominant` 标志捕获「三方向都只围绕合规/仓储」）。
- `industry-recognition-classifier.js`：6 类分类，每方向 ≥2 类、整体须覆盖前 5 类。
- `asset-authorization-evaluator.js`：FORGERY_PATTERNS 扫描注册证号/批次/合格率%/徽章等 → 硬阻断 forgery_detected；暴露 4 授权模式。
- `tests/v5/visual-translation-v2-specialized-fix.test.js`：10 离线用例（好样本 family-distinct / 坏样本原同质 fixture 作负向回归）。

修改：
- `execution-readiness-evaluator.js`：§11 封顶——任一 Blocking Gate / 硬指标失败 → 综合分封顶 59（`score_capped:true`），杜绝 `100/100 + rewrite_required`。
- `direction-contract-v2.js`：新增 `direction_family`(enum A/B/C) / `compliance_weights` / `industry_recognition_classification` / `asset_authorization`；execution_examples 强化字段（touchpoint/audience/communication_goal/hero_subject/industry_content/layout_structure/brand_specific_detail/anti_concept_art_rule）；industry classification 校验 `{min:0}`（关键：允许方向留空部分类别）。
- `compile-execution-direction-v2.js`：按 §13 顺序串联六道 Gate；`hardBlocked = brandIdentity.contamination_detected || assetAuthorizationSet.forgery_detected`；输出 `gates`/`blocking_reasons`/`expected_brand_name`/`overall_status`。**保持 per-direction readiness 纯净**（不被 gate 覆盖），score cap 仅由 evaluator 内部施加——从而零破坏 A/B snapshot。
- `direction-generation-prompt-v2.js`：从 `context.brandFacts.identity` 注入 brandName（默认「九州美学」）/brandRole；新增 PROJECT BRAND 段 + PRINCIPLE 7 十条硬约束（插值 ${brandName}/${brandRole}）；JSON 示例加新字段 + 「三方向分属不同 Family、相似度<0.72」说明。
- `report/compile-execution-directions-report-v2.js`：每方向追加「品牌身份 + 方向家族 + 就绪分封顶」；新增「## 专项修复 Gate 总览」段。

测试与质量门：
- 专项修复 `node --test tests/v5/visual-translation-v2-specialized-fix.test.js` → **10/10**。
- 完整回归 `node --test tests/v5/visual-translation-v2.test.js tests/v5/visual-translation-v2-stage04.test.js tests/v5/visual-translation-v2-specialized-fix.test.js` → **32/32**（22 既有 + 10 新增，无回归）。
- `npm run verify:document-flows` → **PASS**（Desktop TS contracts PASS，12/12 文档流 + Desktop 契约）。
- 遵守 §15-18 约束：未回退 v1、未改 Document Extraction/Evidence/Asset Authorization(原 v1)/Audience Boundary/v1.3.4 Report Compiler；未接图片生成、未进 Anchor Image Exploration、未允许示例品牌进正式输出。

待办/已知限制：
- 品牌 denylist 依赖调用方传入 `knownExampleBrandNames`（真实运行按 §15 真实项目：
  九州美学→名济堂/万科苏皖/梧桐金陵/素问·一剂良方 回归），当前仅离线 fixture 验证。
- 重新打包 portable 客户端（Task #37）进行中，须验证六道新 Gate 代码打进 bundle。

### 关键调试记录（专项修复）
- 品牌名正则初版过激（「医美/美学/机构」误报）→ 收窄为仅强品牌后缀，主检查退为 denylist。
- direction-family-difference 初版 cosine over axis-keyword 误判高相似 → 改 Jaccard over 具体行业词汇（排除 cosmetic subject）。
- `validateOptionalIndustryClassification` 初版 `{min:1}` 致 schema FAILED → 改 `{min:0}`。
- 坏样本 compliance 每方向仅 0.2 不触发原 `all>=0.5` → 新增 `compliance_supplychain_dominant` 标志。
- leaked-brand 子测试须传 `knownExampleBrandNames:['安迹']` 才能检测。
